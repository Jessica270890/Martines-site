name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Hostinger webhook
        run: |
          curl -s -X POST https://webhooks.hostinger.com/deploy/c3481852fcf6e01a2c432dfd73cd55f3
